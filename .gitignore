# ── Environment ──
.env
.env.local
.env.production

# ── Node ──
node_modules/
npm-debug.log*

# ── Next.js ──
.next/
out/
.vercel/

# ── Local-first datastore (never commit user data) ──
data/

# ── WhatsApp session (never commit) ──
.wwebjs_auth/
.wwebjs_cache/

# ── Electron build output ──
dist/

# ── OS ──
.DS_Store
Thumbs.db

# ── PWA generated files ──
public/sw.js
public/workbox-*.js
